{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "references_config.schema.json",
    "title": "GicaTesis References Config Schema",
    "description": "Schema para configuración de referencias por universidad.",
    "type": "object",
    "required": [
        "enabled"
    ],
    "properties": {
        "enabled": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Lista de normas habilitadas (ej: apa7, ieee)"
        },
        "default": {
            "type": "string",
            "description": "Norma por defecto"
        }
    },
    "additionalProperties": true
}